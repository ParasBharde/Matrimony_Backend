'use strict';

/**
 * active-subcription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::active-subcription.active-subcription');
